meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/logout
  body: none
  auth: none
}

tests {
  test("deve encerrar a sessao", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}
