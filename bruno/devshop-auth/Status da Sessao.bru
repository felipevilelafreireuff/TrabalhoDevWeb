meta {
  name: Status da Sessao
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/login
  body: none
  auth: none
}

tests {
  test("deve retornar o status da sessao", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property("authenticated");
  });
}
